<menu xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    xmlns:android="http://schemas.android.com/apk/res/android"

    >

    <item
        android:id="@+id/action_connstatus"
        android:checkable="false"
        android:onClick="onClickConn"
        android:orderInCategory="52"
        android:title="@string/conn_waiting"
        android:visible="true"
        app:showAsAction="always" />
    <item
        android:id="@+id/action_clear_cache"
        android:onClick="onClearCache"
        android:orderInCategory="53"
        android:title="@string/action_clear_cache"></item>
    <item
        android:id="@+id/action_refresh"
        android:icon="@drawable/ic_refresh_white_24px"
        android:onClick="onClickRefresh"
        android:orderInCategory="54"
        android:title="@string/action_refresh"
        android:visible="true"
        app:showAsAction="always" />
</menu>